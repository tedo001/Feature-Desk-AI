from loaders.base_loader import BaseLoader


class TrackerLoader(BaseLoader):
    @classmethod
    def _load_model(cls):
        # Replace with real ByteTrack init when available
        return object()
